{/* Colors.mdx */}
import { Meta } from "@storybook/addon-docs/blocks";
import { PaletteScales, SemanticTokens } from "./ColorReference";

<Meta title="Foundation/Colors" />

# Colors

The cytario palette is built on two brand colors — purple and teal — extended into full scales with neutral grays and semantic mappings for UI states.

Every swatch below is read live from the loaded token stylesheets (`src/tokens/variables.css` and `variables-dark.css`) and resolved under both themes. Nothing on this page is hardcoded, so it cannot drift from the tokens.

---

## Brand colors

<div style={{ display: "flex", gap: 32, marginTop: 24, marginBottom: 48, flexWrap: "wrap" }}>
  <div style={{ textAlign: "center" }}>
    <div style={{
      width: 160,
      height: 160,
      borderRadius: 16,
      background: "var(--color-purple-700)",
      marginBottom: 12,
    }} />
    <p style={{ margin: 0, fontWeight: 700, fontSize: 16 }}>Purple</p>
    <code style={{ fontSize: 13 }}>--color-purple-700</code>
    <p style={{ margin: "4px 0 0", fontSize: 13, opacity: 0.6 }}>Brand primary</p>
  </div>

  <div style={{ textAlign: "center" }}>
    <div style={{
      width: 160,
      height: 160,
      borderRadius: 16,
      background: "var(--color-teal-500)",
      marginBottom: 12,
    }} />
    <p style={{ margin: 0, fontWeight: 700, fontSize: 16 }}>Teal</p>
    <code style={{ fontSize: 13 }}>--color-teal-500</code>
    <p style={{ margin: "4px 0 0", fontSize: 13, opacity: 0.6 }}>Brand accent</p>
  </div>
</div>

---

## Palette scales

Raw color primitives. These are theme-invariant — light and dark resolve to the same values — so each is shown once.

<PaletteScales />

---

## Semantic tokens

Semantic tokens map purpose to palette values, so UI code references intent rather than raw color. These remap per theme, shown as separate light and dark galleries.

<SemanticTokens />

---

## Accessibility and contrast

| Combination | Contrast ratio | WCAG AA (normal) | WCAG AA (large) |
| --- | --- | --- | --- |
| Purple 700 on white | 8.5:1 | Pass | Pass |
| Teal 500 on white | 2.8:1 | Fail | Fail |
| Teal 700 on white | 4.6:1 | Pass (large) | Pass |
| White on purple 700 | 8.5:1 | Pass | Pass |
| Neutral 900 on white | 17.4:1 | Pass | Pass |
| Neutral 600 on white | 5.7:1 | Pass | Pass |

**Key rule:** Teal-500 does not meet WCAG AA contrast on white backgrounds for normal text. Use **teal-700** or darker for text. Teal-500 is suitable for large text, icons, and non-text decorative elements.

---

## Brand governance

**Approved palette:** The purple and teal brand colors were established as part of the cytario corporate design approved on 05.11.2024. Do not introduce new brand colors without approval.

**Color in regulatory contexts:** Regulatory submissions and labels should not rely on color alone to convey meaning (per IEC 62366-1 usability requirements). When color is used in the cytario application UI for diagnostic significance (e.g., annotations, heatmaps), ensure an alternative non-color indicator is also present.
